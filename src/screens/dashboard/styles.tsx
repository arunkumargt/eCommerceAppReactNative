import { StyleSheet } from "react-native";

const dashboardStyleSheet = StyleSheet.create({
    container: {
        flex: 1,
    },
    barStyle: {
        // height: 10
    }
})

export default dashboardStyleSheet;